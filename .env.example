# 复制为 .env 填真实值：cp .env.example .env
# .env 已被 .gitignore 忽略（真实凭据不进仓库）；本文件是模板，可以提交。
# 任何一项留空都不会报错，对应功能走兜底逻辑。

# 腾讯位置服务 Web 服务 key
# 获取地址：https://lbs.qq.com/dev/console/application/mine
# 格式：XXXXX-XXXXX-XXXXX-XXXXX-XXXXX-XXXXX
# 必须带 PUBLIC_ 前缀，Astro 才会把它注入客户端构建；不加前缀则首页渲染出空值。
# 留空 = 不发起 IP 定位请求，公告卡直接显示欢迎语。
#
# 注意：这个 key 会出现在首页 HTML 的 data-map-key 属性里（已知并接受的行为）。
# 所以请在 lbs.qq.com 侧设好每日配额上限和参考应用域名白名单，把它当公开资源管理。
# status 110 = 参考应用域名白名单不含当前域名；status 121 = 该 key 每日配额已用尽。
PUBLIC_MAP_KEY=

# 以下为 GitHub Actions 专用（Vercel 部署不用配这两个），仅在本地跑 CI 时参考：
# SITE_URL=https://cyanvir.cc.cd
# ASTRO_BASE=/
